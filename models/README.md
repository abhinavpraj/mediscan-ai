# Local Models

Place a CPU-compatible GGUF model in this directory.

Recommended filenames:

- `phi-3-mini.gguf`
- `qwen2.5-mini.gguf`

Set `MEDISCAN_MODEL_PATH` when using a different path. Model weights are not committed because they are large and distributed under their own licenses.
